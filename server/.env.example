# Port the API server listens on
PORT=4000

# Host the API binds to
HOST=localhost

# Public base URL used to build absolute asset (image) URLs.
# Leave empty to auto-derive from HOST/PORT (http://HOST:PORT).
PUBLIC_URL=

# Comma-separated list of allowed CORS origins (frontend dev server).
CORS_ORIGIN=http://localhost:5173
